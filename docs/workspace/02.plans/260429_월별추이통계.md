# 월별 추이 통계 화면 구현 플랜

**Goal:** 홈 Total 탭으로 진입하는 통계 화면 — currentMonth 도넛 + 카테고리 리스트 + 월별 비교 바 차트.

**Architecture:** `script.js`에 `renderStats()` 라우트 추가, 헬퍼 `getMonthTotals()`/`donutSlicePath()`/`donutColor()` 추가. SVG 직접 그리기, 의존성 무도입. CSS는 `style.css`에 `.stats-*`, `.donut-*`, `.cat-row`, `.month-bar*` 추가.

**Tech Stack:** Vanilla JS, SVG, CSS — 기존 패턴 그대로.

**Spec:** `docs/workspace/01.specs/260429_월별추이통계.md`

---

## 파일 구조

- **Modify** `script.js`:
  - L94 `parseRoute()`: `'stats'` 케이스 추가
  - L114 `render()`: `else if (route.name === 'stats') renderStats();`
  - L70~ 헬퍼 영역: `getMonthTotals()`, `donutSlicePath()`, `donutFullPath()`, `donutColor()` 추가
  - L122 `renderHome()`: Total 영역 클릭 핸들러 추가 (지출 0건이면 비활성)
  - 새 함수 `renderStats()` 추가 (renderCategory 다음 위치)
- **Modify** `style.css`:
  - 끝부분에 `/* ============ STATS ============ */` 섹션 추가
  - `.summary` 블록: `.summary.is-tappable` 보조 스타일

새 파일 없음.

---

## Task 1 — 헬퍼 함수 추가

**Files:** `script.js`

- [ ] **Step 1: utils 영역(L70 위)에 `getMonthTotals` 추가**

```js
function getMonthTotals() {
  const map = new Map();
  for (const e of state.expenses) {
    const m = e.date.slice(0, 7);
    map.set(m, (map.get(m) || 0) + e.amount);
  }
  return [...map.entries()]
    .sort(([a], [b]) => a.localeCompare(b))
    .slice(-12)
    .map(([month, total]) => ({ month, total }));
}
```

- [ ] **Step 2: SVG 도넛 path 헬퍼 추가**

```js
function donutSlicePath(startAngle, endAngle, rOuter, rInner) {
  const cx = 50, cy = 50;
  const x1 = cx + rOuter * Math.sin(startAngle);
  const y1 = cy - rOuter * Math.cos(startAngle);
  const x2 = cx + rOuter * Math.sin(endAngle);
  const y2 = cy - rOuter * Math.cos(endAngle);
  const x3 = cx + rInner * Math.sin(endAngle);
  const y3 = cy - rInner * Math.cos(endAngle);
  const x4 = cx + rInner * Math.sin(startAngle);
  const y4 = cy - rInner * Math.cos(startAngle);
  const largeArc = (endAngle - startAngle) > Math.PI ? 1 : 0;
  return `M ${x1.toFixed(3)} ${y1.toFixed(3)}
          A ${rOuter} ${rOuter} 0 ${largeArc} 1 ${x2.toFixed(3)} ${y2.toFixed(3)}
          L ${x3.toFixed(3)} ${y3.toFixed(3)}
          A ${rInner} ${rInner} 0 ${largeArc} 0 ${x4.toFixed(3)} ${y4.toFixed(3)} Z`;
}

function donutFullPath(rOuter, rInner) {
  const cx = 50, cy = 50;
  return `M ${cx - rOuter} ${cy} A ${rOuter} ${rOuter} 0 1 0 ${cx + rOuter} ${cy} A ${rOuter} ${rOuter} 0 1 0 ${cx - rOuter} ${cy} Z M ${cx - rInner} ${cy} A ${rInner} ${rInner} 0 1 1 ${cx + rInner} ${cy} A ${rInner} ${rInner} 0 1 1 ${cx - rInner} ${cy} Z`;
}

function donutColor(idx, total) {
  if (total <= 1) return 'hsl(36 12% 18%)';
  const L = 18 + (idx / (total - 1)) * 44;
  return `hsl(36 12% ${L.toFixed(1)}%)`;
}
```

---

## Task 2 — 라우팅 추가

**Files:** `script.js:94-100`

- [ ] **Step 1: `parseRoute()`에 stats 케이스**

```js
function parseRoute() {
  const h = location.hash.slice(2);
  if (h === 'stats') return { name: 'stats' };
  if (h.startsWith('category/')) {
    return { name: 'category', cat: decodeURIComponent(h.slice(9)) };
  }
  return { name: 'home' };
}
```

- [ ] **Step 2: `render()` 분기 추가**

```js
function render() {
  const route = parseRoute();
  app.innerHTML = '';
  if (route.name === 'home') renderHome();
  else if (route.name === 'category') renderCategory(route.cat);
  else if (route.name === 'stats') renderStats();
}
```

---

## Task 3 — `renderStats()` 추가

**Files:** `script.js` (renderCategory 다음 위치)

- [ ] **Step 1: 함수 본체 추가**

```js
function renderStats() {
  const month = state.currentMonth;
  const totals = getCategoryTotals(month);
  const sumTotal = Object.values(totals).reduce((a, b) => a + b, 0);
  const { name, year } = monthLabel(month);

  // 헤더 (홈과 동일)
  const header = el(`
    <header class="header">
      <button class="month-nav" id="prev-month" aria-label="이전 달">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
      </button>
      <div class="month-display">
        <div class="month-year">${year}</div>
        <div class="month-name">${name}</div>
      </div>
      <button class="month-nav" id="next-month" aria-label="다음 달">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
      </button>
    </header>
  `);
  app.appendChild(header);

  // 뒤로가기 (디테일 헤더 아래에 작게 — 홈으로 돌아가는 버튼이 따로 필요)
  const backRow = el(`
    <div class="stats-back">
      <button class="back-btn" id="back-btn" aria-label="뒤로">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
      </button>
      <div class="stats-back-label">통계</div>
      <div></div>
    </div>
  `);
  app.appendChild(backRow);

  // 도넛 섹션
  const sortedCats = CATEGORIES
    .map(c => ({ cat: c, amount: totals[c] }))
    .filter(x => x.amount > 0)
    .sort((a, b) => b.amount - a.amount);

  const donutSection = el(`<section class="stats-section donut-section"></section>`);
  donutSection.appendChild(el(`<div class="section-label inline"><span>이번 달 사용처</span></div>`));

  if (sortedCats.length === 0) {
    donutSection.appendChild(el(`
      <div class="donut-empty">
        <div class="empty-state-mark">∅</div>
        <div class="empty-state-text">이번 달 지출 없음</div>
      </div>
    `));
  } else {
    // SVG 도넛
    const svgNS = 'http://www.w3.org/2000/svg';
    const svg = document.createElementNS(svgNS, 'svg');
    svg.setAttribute('class', 'donut-svg');
    svg.setAttribute('viewBox', '0 0 100 100');
    svg.setAttribute('width', '220');
    svg.setAttribute('height', '220');

    const rOuter = 42, rInner = 26;
    if (sortedCats.length === 1) {
      const path = document.createElementNS(svgNS, 'path');
      path.setAttribute('d', donutFullPath(rOuter, rInner));
      path.setAttribute('fill', donutColor(0, 1));
      path.setAttribute('fill-rule', 'evenodd');
      path.style.cursor = 'pointer';
      path.addEventListener('click', () => navigate('category/' + encodeURIComponent(sortedCats[0].cat)));
      svg.appendChild(path);
    } else {
      let acc = 0;
      sortedCats.forEach((s, i) => {
        const angleStart = (acc / sumTotal) * Math.PI * 2;
        acc += s.amount;
        const angleEnd = (acc / sumTotal) * Math.PI * 2;
        const path = document.createElementNS(svgNS, 'path');
        path.setAttribute('d', donutSlicePath(angleStart, angleEnd, rOuter, rInner));
        path.setAttribute('fill', donutColor(i, sortedCats.length));
        path.style.cursor = 'pointer';
        path.addEventListener('click', () => navigate('category/' + encodeURIComponent(s.cat)));
        svg.appendChild(path);
      });
    }

    const wrap = el(`<div class="donut-wrap"></div>`);
    wrap.appendChild(svg);
    wrap.appendChild(el(`
      <div class="donut-center">
        <div class="donut-center-label">Total</div>
        <div class="donut-center-amount"><span class="currency">₩</span>${fmt(sumTotal)}</div>
      </div>
    `));
    donutSection.appendChild(wrap);

    // 카테고리 리스트
    const list = el(`<ul class="cat-list"></ul>`);
    sortedCats.forEach((s, i) => {
      const pct = Math.round((s.amount / sumTotal) * 100);
      const row = el(`
        <li class="cat-row" data-cat="${escapeHtml(s.cat)}" style="animation-delay: ${30 + i * 30}ms">
          <span class="cat-chip-color" style="background: ${donutColor(i, sortedCats.length)}"></span>
          <span class="cat-row-name">${escapeHtml(s.cat)}</span>
          <span class="cat-row-amount">${fmt(s.amount)}</span>
          <span class="cat-row-pct">${pct}%</span>
        </li>
      `);
      row.addEventListener('click', () => navigate('category/' + encodeURIComponent(s.cat)));
      list.appendChild(row);
    });
    donutSection.appendChild(list);
  }

  app.appendChild(donutSection);

  // 월별 비교 섹션
  const monthTotals = getMonthTotals();
  if (monthTotals.length > 1) {
    const compareSection = el(`<section class="stats-section compare-section"></section>`);
    compareSection.appendChild(el(`<div class="section-label inline"><span>월별 비교</span></div>`));

    const max = Math.max(...monthTotals.map(m => m.total));
    const bars = el(`<div class="month-bars"></div>`);
    monthTotals.forEach((m, i) => {
      const h = max > 0 ? (m.total / max) * 100 : 0;
      const isCurrent = m.month === state.currentMonth;
      const [my, mm] = m.month.split('-');
      const prevMonth = i > 0 ? monthTotals[i - 1].month.split('-')[0] : null;
      const showYear = prevMonth !== my;
      const item = el(`
        <button class="month-bar-item ${isCurrent ? 'is-current' : ''}" data-month="${m.month}" style="animation-delay: ${60 + i * 50}ms">
          <span class="month-bar-track">
            <span class="month-bar-fill" style="--target: ${(h / 100).toFixed(3)}"></span>
          </span>
          <span class="month-bar-label">${showYear ? `${my.slice(2)}.${mm}` : mm}</span>
        </button>
      `);
      item.addEventListener('click', () => setMonth(m.month));
      bars.appendChild(item);
    });
    compareSection.appendChild(bars);
    app.appendChild(compareSection);
  }

  app.appendChild(el(`<div class="brand">a budget journal</div>`));

  header.querySelector('#prev-month').addEventListener('click', () => changeMonth(-1));
  header.querySelector('#next-month').addEventListener('click', () => changeMonth(1));
  header.querySelector('.month-display').addEventListener('click', () => openMonthPicker());
  app.querySelector('#back-btn').addEventListener('click', () => navigate(''));
}
```

---

## Task 4 — 홈 Total 탭 진입점

**Files:** `script.js:145-151` (renderHome 안의 summary 생성부)

- [ ] **Step 1: 데이터 0건이면 비활성, 아니면 클릭 시 stats 라우트로**

```js
const hasAnyExpense = state.expenses.length > 0;
const summary = el(`
  <section class="summary ${hasAnyExpense ? 'is-tappable' : ''}">
    <div class="summary-label">Total</div>
    <div class="summary-amount"><span class="currency">₩</span>${fmt(sumTotal)}</div>
  </section>
`);
if (hasAnyExpense) {
  summary.addEventListener('click', () => navigate('stats'));
}
app.appendChild(summary);
```

---

## Task 5 — CSS 추가

**Files:** `style.css`

- [ ] **Step 1: `.summary.is-tappable` 보조 스타일 추가** (.summary 블록 직후)

```css
.summary.is-tappable {
  cursor: pointer;
  margin: 0 calc(var(--pad) - 6px) 18px;
  padding: 6px 6px 22px;
  border-radius: 10px;
  transition: background 180ms var(--ease-snap);
}
.summary.is-tappable:active { background: var(--rule-soft); }
```

- [ ] **Step 2: STATS 섹션을 파일 맨 끝에 추가**

```css
/* ============ STATS ============ */
.stats-back {
  padding: 6px var(--pad) 12px;
  display: grid;
  grid-template-columns: 40px 1fr 40px;
  align-items: center;
  gap: 12px;
  border-bottom: 1px solid var(--rule);
}
.stats-back-label {
  text-align: center;
  font-family: var(--font-body);
  font-size: 10px;
  font-weight: 500;
  color: var(--ink-mute);
  letter-spacing: 0.22em;
  text-transform: uppercase;
}

.stats-section {
  padding-bottom: 22px;
  border-bottom: 1px solid var(--rule);
}
.stats-section:last-of-type {
  border-bottom: none;
  padding-bottom: 100px;
}
.section-label.inline {
  padding: 22px var(--pad) 14px;
}

.donut-wrap {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 4px 0 22px;
}
.donut-svg {
  display: block;
  animation: donut-in 0.6s var(--ease-out) backwards;
  animation-delay: 0.1s;
}
@keyframes donut-in {
  from { opacity: 0; transform: scale(0.94); }
  to { opacity: 1; transform: scale(1); }
}
.donut-svg path {
  transition: opacity 180ms var(--ease-snap);
}
.donut-svg path:active { opacity: 0.7; }
.donut-center {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  pointer-events: none;
}
.donut-center-label {
  font-family: var(--font-body);
  font-size: 9px;
  font-weight: 500;
  color: var(--ink-mute);
  letter-spacing: 0.24em;
  text-transform: uppercase;
  margin-bottom: 4px;
}
.donut-center-amount {
  font-family: var(--font-body);
  font-size: 18px;
  font-weight: 600;
  letter-spacing: -0.022em;
  font-variant-numeric: tabular-nums;
}
.donut-center-amount .currency {
  font-size: 11px;
  color: var(--ink-mute);
  font-weight: 500;
  margin-right: 2px;
  vertical-align: 0.08em;
}

.donut-empty {
  text-align: center;
  padding: 30px 40px 20px;
}
.donut-empty .empty-state-mark {
  font-size: 72px;
  margin-bottom: 8px;
}

.cat-list {
  list-style: none;
  margin: 0;
  padding: 0 var(--pad);
}
.cat-row {
  display: grid;
  grid-template-columns: 8px 1fr auto auto;
  align-items: center;
  gap: 12px;
  padding: 11px 0;
  border-top: 1px solid var(--rule-soft);
  cursor: pointer;
  animation: entry-in 0.5s var(--ease-out) backwards;
  transition: background 180ms var(--ease-snap), padding 180ms;
}
.cat-row:first-child { border-top: none; }
.cat-row:active {
  background: var(--rule-soft);
}
.cat-chip-color {
  display: block;
  width: 8px;
  height: 8px;
  border-radius: 2px;
}
.cat-row-name {
  font-size: 14px;
  font-weight: 500;
  color: var(--ink);
  letter-spacing: -0.005em;
}
.cat-row-amount {
  font-family: var(--font-body);
  font-size: 14px;
  font-weight: 600;
  letter-spacing: -0.018em;
  font-variant-numeric: tabular-nums;
  color: var(--ink);
}
.cat-row-pct {
  font-family: var(--font-body);
  font-size: 11px;
  font-weight: 500;
  color: var(--ink-mute);
  font-variant-numeric: tabular-nums;
  letter-spacing: 0.02em;
  min-width: 36px;
  text-align: right;
}

.month-bars {
  display: flex;
  align-items: flex-end;
  gap: 6px;
  padding: 14px var(--pad) 0;
  height: 132px;
}
.month-bar-item {
  flex: 1;
  background: transparent;
  border: none;
  padding: 0;
  cursor: pointer;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  height: 100%;
  gap: 8px;
  animation: entry-in 0.5s var(--ease-out) backwards;
  -webkit-tap-highlight-color: transparent;
}
.month-bar-track {
  display: block;
  flex: 1;
  position: relative;
  border-radius: 4px;
  overflow: hidden;
  background: var(--rule-soft);
}
.month-bar-fill {
  position: absolute;
  left: 0; right: 0; bottom: 0;
  background: var(--rule);
  border-radius: 4px;
  transform-origin: bottom;
  transform: scaleY(0);
  animation: bar-rise 0.7s var(--ease-out) forwards;
  animation-delay: 0.18s;
}
.month-bar-item.is-current .month-bar-fill {
  background: var(--ink);
}
.month-bar-item:active .month-bar-track {
  background: var(--rule);
}
.month-bar-label {
  font-family: var(--font-body);
  font-size: 10px;
  font-weight: 500;
  color: var(--ink-mute);
  letter-spacing: 0.04em;
  font-variant-numeric: tabular-nums;
  text-align: center;
}
.month-bar-item.is-current .month-bar-label {
  color: var(--ink);
  font-weight: 600;
}

@keyframes bar-rise {
  to { transform: scaleY(var(--target, 1)); }
}
```

---

## Task 6 — 검증 / 커밋

- [ ] **Step 1: 데스크톱 브라우저에서 device toolbar로 시각 검증**
  - 홈 → Total 탭 → stats 화면 진입 확인
  - 도넛 슬라이스 색 그라데이션 (진한→옅은) 확인
  - 카테고리 리스트가 도넛 색과 일치하는지
  - 월별 비교 바에서 currentMonth만 진한 톤
  - 바 탭 → 그 달로 이동 + 화면 갱신
  - 도넛 슬라이스/카테고리 행 탭 → 카테고리 상세
  - 빈 데이터 케이스: 새 incognito에서 expenses 없을 때 Total 탭이 안 먹는지
  - 모바일 viewport(380px)에서 도넛/바 레이아웃 깨짐 없는지

- [ ] **Step 2: 커밋**

```bash
git add script.js style.css docs/workspace
git commit -m "feat: monthly stats screen with donut + comparison bars"
```

---

## 비목표 (이번 라운드 외)

- 카테고리별 월 추이 라인 (drill-down)
- 예산 대비 달성률
- 6/12 토글
- 통계 화면 내 FAB
